name: Security Flaws

on: [push]

permissions:
  contents: write-all

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      actions: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run a command
      run: echo 'Running secure command'
