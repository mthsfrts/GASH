name: Duplicate Jobs

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          npm install
          npm test

  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          npm install
          npm test
